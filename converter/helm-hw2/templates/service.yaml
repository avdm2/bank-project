apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-hw2.fullname" . }}
  labels:
    {{- include "helm-hw2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "helm-hw2.selectorLabels" . | nindent 4 }}
